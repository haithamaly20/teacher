# برومبت بناء نظام متابعة الرياضيات — 5 مراحل

---

## السياق العام

أنت مطور ويب محترف متخصص في بناء أنظمة إدارة مدرسية. ستبني نظامًا إلكترونيًا متكاملًا لمتابعة وحصر معلمي وفصول الرياضيات واحتياجات القوافل التعليمية في المعاهد الإعدادية والثانوية التابعة لمديرية مطروح الأزهرية.

**المواصفات التقنية الثابتة لكل المراحل:**
- ملف HTML واحد لكل مرحلة (self-contained)
- التخزين: Google Sheets عبر Google Apps Script (Web App منشور كـ doGet/doPost)
- اللغة: عربية كاملة، اتجاه RTL صارم على مستوى الصفحة والجداول والنماذج
- الأرقام: إنجليزية (0-9)
- التصميم: Bootstrap 5 RTL من CDN، نظيف واحترافي
- لا قواعد بيانات خارجية، لا خوادم، لا مصادقة — مستخدم واحد
- كل منطق الحساب في JavaScript على جانب العميل
- Google Sheets هي المخزن الوحيد للبيانات
- كل ملف HTML يعمل مع نفس Google Apps Script الـ Web App URL

---

## القاعدة المنطقية الصارمة (لا تُخالَف في أي مرحلة)

```
حالة الفصل = ثنائية حصرية:
  إذا كان للفصل معلم (رياضيات أو سد عجز) → 🟢 مكتفي
  إذا لم يكن للفصل معلم                   → 🔴 يحتاج قوافل

الحصص الزائدة  = MAX(0, الحصص الفعلية − النصاب القانوني)
العجز عن النصاب = MAX(0, النصاب القانوني − الحصص الفعلية)
لا يمكن أن يكون كلاهما موجبًا في آنٍ واحد
```

---

## بنية Google Sheets (ثابتة من المرحلة الأولى)

### الشيت 1: `institutes`
| id | name | type | notes |
|----|------|------|-------|
| UUID | اسم المعهد | إعدادي / ثانوي / إعدادي وثانوي | ملاحظات |

### الشيت 2: `classes`
| id | institute_id | grade | class_number | teacher_id | status |
|----|-------------|-------|-------------|------------|--------|
| UUID | FK→institutes | الأول الإعدادي...الثالث الثانوي | 1,2,3... | FK→teachers أو فارغ | مكتفي / يحتاج قوافل |

### الشيت 3: `teachers`
| id | name | institute_id | teacher_type | cadre_grade | legal_quota | actual_hours | math_hours | deficit_hours | extra_hours | supervision | notes |
|----|------|-------------|-------------|------------|------------|-------------|-----------|--------------|------------|------------|-------|
| UUID | الاسم | FK→institutes | معلم رياضيات / معلم يسد عجز | درجة الكادر | رقم | رقم | رقم | محسوب | محسوب | رقم | نص |

### الشيت 4: `visits`
| id | institute_id | visit_date | visitor_name | sufficient_classes | needs_convoy_classes | notes | snapshot_json |
|----|-------------|-----------|-------------|-------------------|---------------------|-------|---------------|
| UUID | FK→institutes | تاريخ | اسم | رقم | رقم | نص | JSON كامل للحظة الزيارة |

### الشيت 5: `settings`
| key | value |
|-----|-------|
| cadre_grades | JSON array لدرجات الكادر |
| grades_list | JSON array للصفوف الستة |

---

## المرحلة الأولى — البنية الأساسية والبيئة

### الهدف
إنشاء Google Apps Script كـ Web App، وبناء ملف HTML أساسي يثبت الاتصال بـ Google Sheets.

### مهام Apps Script
اكتب `Code.gs` كاملًا يحتوي على:

**doGet(e):** يُرجع JSON بقائمة جميع الشيتات للتحقق من الاتصال.

**doPost(e):** يستقبل JSON بالشكل `{ action, payload }` ويوجّه إلى الدوال التالية:

```javascript
// الدوال المطلوبة في المرحلة الأولى فقط:
createInstitute(payload)   // إضافة صف جديد في institutes
getInstitutes()            // جلب كل الصفوف
updateInstitute(payload)   // تعديل صف بحسب id
deleteInstitute(id)        // حذف صف بحسب id
initSheets()               // إنشاء الشيتات الخمسة إن لم تكن موجودة + بيانات settings الافتراضية
```

**بيانات settings الافتراضية:**
```json
{
  "cadre_grades": ["معلم مساعد","معلم","معلم أول","معلم أول (أ)","معلم خبير","كبير معلمين"],
  "grades_list": ["الأول الإعدادي","الثاني الإعدادي","الثالث الإعدادي","الأول الثانوي","الثاني الثانوي","الثالث الثانوي"]
}
```

### مهام ملف HTML — `phase1.html`
صفحة إعداد وإدارة المعاهد تحتوي على:

1. **حقل إدخال Web App URL** محفوظ في `localStorage` تحت مفتاح `GAS_URL` — يظهر في أعلى كل صفحة
2. **زر "تهيئة قاعدة البيانات"** يستدعي `initSheets` مع مؤشر تحميل ورسالة نجاح/خطأ
3. **نموذج إضافة معهد:** اسم المعهد + نوعه (قائمة: إعدادي / ثانوي / إعدادي وثانوي) + ملاحظات
4. **جدول المعاهد** يعرض: الاسم، النوع، الملاحظات، زر تعديل، زر حذف
5. **التعديل inline** داخل الجدول مباشرة بدون modal
6. **البحث الفوري** في جدول المعاهد

**متطلبات تقنية للملف:**
- دالة `callGAS(action, payload)` مركزية لكل الاستدعاءات
- معالجة الأخطاء: رسائل واضحة بالعربية لكل حالة (خطأ في الشبكة، بيانات ناقصة، فشل الحفظ)
- تحميل قائمة المعاهد تلقائيًا عند فتح الصفحة إذا كان `GAS_URL` موجودًا

---

## المرحلة الثانية — الفصول والمعلمون

### الهدف
إدارة الفصول وإدارة المعلمين مع الحساب التلقائي الفوري.

### مهام إضافية في Apps Script

```javascript
// فصول
createClasses(institute_id, grade, count)  // ينشئ (count) فصلًا دفعة واحدة
getClasses(institute_id)                   // جلب فصول معهد معين
assignTeacher(class_id, teacher_id)        // ربط معلم بفصل + تحديث status تلقائيًا
unassignTeacher(class_id)                  // إزالة المعلم + تحديث status

// معلمون
createTeacher(payload)
getTeachers(institute_id)
updateTeacher(payload)
deleteTeacher(id)
```

### مهام ملف HTML — `phase2.html`

**قسم الفصول:**
- اختيار معهد من قائمة منسدلة
- لكل صف من الصفوف الستة: حقل عدد الفصول + زر "إنشاء"
- جدول الفصول يعرض: الصف، رقم الفصل، المعلم المرتبط، الحالة (🟢/🔴)
- ربط المعلم بالفصل من قائمة منسدلة تحتوي معلمي المعهد فقط
- الحالة تتحدث فور الربط أو الإزالة بدون إعادة تحميل الصفحة

**قسم المعلمين:**
- نموذج إضافة معلم: الاسم، نوع المعلم (رياضيات / سد عجز)، المعهد، درجة الكادر (من settings)، النصاب القانوني، الحصص الفعلية، حصص الرياضيات، حصص سد العجز، الإشراف، ملاحظات
- الحساب الفوري على جانب العميل أثناء الإدخال:
  ```
  الحصص الزائدة   = يظهر فورًا بمجرد تغيير الحصص الفعلية أو النصاب
  العجز عن النصاب = يظهر فورًا
  ```
- جدول المعلمين: الاسم، النوع، الدرجة، النصاب، الفعلي، الزائد، العجز، سد العجز، الإشراف، تعديل، حذف
- بطاقة تفصيلية عند الضغط على اسم المعلم تعرض كل بياناته والفصول المرتبطة به

---

## المرحلة الثالثة — لوحات التحكم والإحصائيات

### الهدف
لوحة تحكم المعهد + لوحة التحكم العامة + تحليل العجز حسب الصف.

### مهام إضافية في Apps Script

```javascript
getInstituteStats(institute_id)   // يُرجع إحصائيات معهد واحد
getGlobalStats()                  // يُرجع إحصائيات جميع المعاهد
getGradeAnalysis()                // يُرجع تحليل الفصول مصنّفة حسب الصف
```

### مهام ملف HTML — `phase3.html`

**لوحة تحكم المعهد** (تُفتح باختيار معهد):
- بطاقات إحصائية: إجمالي الفصول، مكتفي، يحتاج قوافل، نسبة الاكتفاء (%)
- عدد معلمي الرياضيات، عدد معلمي سد العجز
- قائمة الفصول المحتاجة للقوافل مع الصف ورقم الفصل

**لوحة التحكم العامة:**
- بطاقات: عدد المعاهد، إجمالي الفصول، فصول مكتفية، فصول تحتاج قوافل
- بطاقات الحصص: إجمالي الحصص الزائدة، إجمالي العجز، إجمالي حصص سد العجز
- جدول المعاهد مع إحصائيات كل معهد في سطر واحد

**تحليل العجز حسب الصف:**
```
جدول بالأعمدة: الصف | إجمالي الفصول | مكتفي | يحتاج قوافل | نسبة الاكتفاء
صف بلون مختلف لكل مرحلة (إعدادي / ثانوي)
```

---

## المرحلة الرابعة — الزيارات والتقارير والبحث

### الهدف
سجل الزيارات الميدانية + التقارير الكاملة + البحث والتصفية المتقدمة.

### مهام إضافية في Apps Script

```javascript
createVisit(payload)              // حفظ زيارة مع snapshot_json للحظة الزيارة
getVisits(institute_id)           // جلب زيارات معهد معين
getVisitHistory(institute_id)     // جدول مقارنة بين الزيارات
getAllVisits()                     // جميع الزيارات لجميع المعاهد
```

### مهام ملف HTML — `phase4.html`

**سجل الزيارات:**
- نموذج تسجيل زيارة: المعهد، التاريخ، اسم القائم بالزيارة، ملاحظات
- عند الحفظ: يُلتقط `snapshot_json` كاملًا من البيانات الحالية للمعهد ويُحفظ مع الزيارة
- جدول الزيارات لكل معهد مع عرض التطور: كم فصلًا كان يحتاج قوافل في كل زيارة
- مقارنة بين زيارتين: الفرق في عدد الفصول المكتفية والمحتاجة للقوافل

**التقارير:**
- **تقرير القوافل:** جدول بكل الفصول غير المغطاة (المعهد، الصف، الفصل) — قابل للتصفية
- **تقرير المعلمين:** جدول شامل (الاسم، الدرجة، النصاب، الفعلي، الزائد، سد العجز، الإشراف)
- **تقرير الحصص:** إحصائيات الحصص مجمّعة بالمعهد
- **تقرير الزيارات:** تاريخ كل المعاهد

**البحث والتصفية المتقدمة:**
- بحث نصي فوري (client-side) عن: معهد، معلم، صف، فصل
- فلاتر: المرحلة، الصف، حالة الفصل، نوع المعلم، درجة الكادر، وجود حصص زائدة، وجود عجز

---

## المرحلة الخامسة — الطباعة والتصدير والتشطيب

### الهدف
تجهيز النظام للاستخدام الفعلي: طباعة احترافية + نسخ احتياطي + واجهة موحدة نهائية.

### مهام ملف HTML — `phase5.html` (الملف النهائي الموحد)

**الطباعة:**
- `@media print` مخصص لكل تقرير: RTL، A4، بدون أزرار، مع عنوان التقرير والتاريخ والإجماليات
- خانات توقيع أو اعتماد في أسفل تقارير القوافل والزيارات
- كل تقرير له زر "طباعة" يُفعّل `window.print()` بعد إخفاء عناصر الشاشة

**النسخ الاحتياطي:**
- زر "تصدير JSON": يجلب كل البيانات من Google Sheets ويحفظها في ملف `.json` محليًا
- زر "استيراد JSON": يقرأ ملف JSON ويُعيد رفع كل البيانات إلى Google Sheets (مع تأكيد قبل الاستبدال)

**الواجهة الموحدة النهائية:**
- شريط تنقل علوي ثابت بالعربية: لوحة التحكم | المعاهد | المعلمون | الفصول | القوافل | الزيارات | التقارير | الإعدادات
- كل قسم يُحمَّل ضمن الصفحة الواحدة (SPA بسيط بـ JavaScript)
- صفحة الإعدادات: تعديل درجات الكادر، تعديل Web App URL
- مؤشر اتصال بـ Google Sheets في الشريط العلوي (🟢 متصل / 🔴 غير متصل)
- حالة آخر مزامنة مع التاريخ والوقت

### متطلبات جودة المرحلة الخامسة
- مراجعة كل الحسابات ومطابقتها مع القاعدة المنطقية الصارمة
- اختبار سيناريوهات الحافة: معلم بدون فصول، فصل بدون معهد، نصاب = صفر
- تحسين الأداء: تخزين مؤقت للبيانات في `sessionStorage` لتجنب الاستدعاءات المتكررة
- رسائل خطأ شاملة بالعربية لكل سيناريو ممكن

---

## تعليمات عامة صارمة لكل المراحل

1. **لا تختصر الكود** — اكتب كل سطر كاملًا قابلًا للتشغيل فورًا
2. **UUID** يُولَّد بـ `crypto.randomUUID()` على جانب العميل قبل الإرسال
3. **كل استدعاء لـ Google Sheets** يمر عبر `doPost` بـ `fetch` مع `mode: 'no-cors'` أو CORS مضبوط في Apps Script عبر `ContentService`
4. **Apps Script** يُرجع دائمًا `ContentService.createTextOutput(JSON.stringify(result)).setMimeType(ContentService.MimeType.JSON)` مع header `Access-Control-Allow-Origin: *`
5. **لا modal لتأكيد الحذف** — استخدم `confirm()` المدمج في المتصفح
6. **Bootstrap 5 RTL** من: `https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css`
7. **كل حقل محسوب** (الحصص الزائدة، العجز، الحالة) لا يُرسَل للـ Sheets — يُحسَب عند الجلب على جانب العميل
8. **تسلسل المراحل صارم** — كل مرحلة تبني على السابقة وتستخدم نفس `GAS_URL` ونفس بنية الشيتات

---

## طريقة استخدام هذا البرومبت

أرسل هذا البرومبت كاملًا ثم أضف في النهاية:

```
ابدأ المرحلة [رقم المرحلة]:
[أي تفاصيل إضافية أو تعديلات خاصة بهذه المرحلة]
```

النظام سيُنتج لكل مرحلة:
- كود `Code.gs` كاملًا (أو الإضافات عليه)
- ملف HTML كاملًا جاهزًا للتشغيل
- تعليمات النشر خطوة بخطوة
